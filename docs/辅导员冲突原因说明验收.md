# 辅导员冲突原因说明

2026-09-04，已完成并上线。版本formal-v4.1.1，只补充解释，不放宽匹配规则或修改已发布结果。

## 显示变化

- 搭档内部冲突：写明双方姓名、吸烟习惯与接受范围，或所选住宿底线与对方相关结构化答案。
- 深夜声音：列出实际命中的游戏时段、开麦、外放、游戏音量或可能因游戏熬夜选项。只说明匹配条件，不将推断当作实际行为定论。
- 人数不足、搭档人数结构限制与其他条件冲突分开显示；搜索未找到组合不声称全局无解。
- 待处理原因自动换行、字号增大。仅辅导员预览与后台草案保存这些说明，不增加学生接口的返回字段。
- 更新shared及cloudfunction两份allocation-core；自由文本和特殊需求仍不进入自动判定。

## 验收证据

- npm test：58/58通过，npm run build成功。
- 线上静态文件与dorm-api部署成功；线上页面6/6通过，测试使用虚构接口数据，没有发布或写入真实分配。
- 首轮线上浏览器测试因独立浏览器进程关闭中断；新进程完整重跑通过。
- 真实58份问卷只读模拟，48人建议入寝、10人待处理，漏人0、拆组0。四班两人吸烟冲突、两人深夜声音冲突、另两人人数不足；一班3人和二班1人人数不足。
- 部署前后学生、问卷、组队、住宿记录内容指纹一致，现有24条固定组发布记录完全保留。

## 使用方法

1. 刷新或重新打开辅导员网站。
2. 进入“分寝建议”，选择班级，再点击“生成分寝建议”。旧浏览器预览不会自动改写。
3. 到页面底部“待辅导员处理”查看具体原因。只查看原因不需要点击“确认发布”。

## 本地复查方法

测试文件已保存为.test.js，无需重新保存或改后缀。右键Windows开始菜单打开终端，输入：

`cd "C:\Users\liu\Documents\Codex\2026-09-01\z-g\outputs\宿舍协同系统联网版"`

再输入 `npm test`，看到58项通过、fail 0即验证成功。需要重建静态文件时使用 `npm run build`，它只重建本地dist；部署与发布住宿结果不是同一操作。

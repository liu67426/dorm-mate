{
  "name": "dorm-mate",
  "version": "0.2.0",
  "private": true,
  "description": "宿舍协同系统:生活习惯问卷、自由组队、自动分寝建议(CloudBase + PostgreSQL)",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test tests/*.test.js",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cloudbase/cli": "^3.8.1",
    "playwright-core": "^1.55.0"
  }
}

{
  "name": "dormitory-coordination-pilot",
  "version": "0.1.0",
  "private": true,
  "description": "工业机器人1至4班宿舍问卷、组队与分配一周试运行版",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test tests/*.test.js",
    "serve": "python -m http.server 8770 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cloudbase/cli": "^3.8.1",
    "playwright-core": "^1.55.0"
  }
}
